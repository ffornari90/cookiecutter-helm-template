{
  "setup_type": [
    "default",
    "custom"
  ],
  "chart_name": "my-chart",
  "dep_chart_name": "",
  "dep_chart_version": "",
  "dep_chart_repository": "",
  "deployment_image_repo": "{% if cookiecutter.setup_type == 'default' %}your-registry/example-app{% endif %}",
  "deployment_image_tag": "{% if cookiecutter.setup_type == 'default' %}latest{% endif %}",
  "additional_resources": "{% if cookiecutter.setup_type == 'default' %}[]{% endif %}",
  "include_databases": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_deployments": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_services": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_ingresses": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_configmaps": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_statefulsets": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_daemonsets": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_jobs": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "include_cronjobs": "{% if cookiecutter.setup_type == 'custom' %}no{% endif %}",
  "__prompts__": {
    "setup_type": "Choose setup type (default/custom)",
    "chart_name": "Name of your Helm chart",
    "dep_chart_name": "Name of the dependency chart you want to install (leave empty if none)",
    "dep_chart_version": "Version of the dependency chart you want to install (leave empty if none)",
    "dep_chart_repository": "Repository of the dependency chart you want to install (leave empty if none)",
    "deployment_image_repo": "Image repository for deployment",
    "deployment_image_tag": "Image tag for deployment",
    "additional_resources": "Additional resources to include (comma-separated list from: databases,configmaps,statefulsets,daemonsets,jobs,cronjobs)",
    "include_databases": "Include example database configuration? (yes/no)",
    "include_deployments": "Include example deployment configuration? (yes/no)",
    "include_services": "Include example service configuration? (yes/no)",
    "include_ingresses": "Include example ingress configuration? (yes/no)",
    "include_configmaps": "Include example configmap configuration? (yes/no)",
    "include_statefulsets": "Include example statefulset configuration? (yes/no)",
    "include_daemonsets": "Include example daemonset configuration? (yes/no)",
    "include_jobs": "Include example job configuration? (yes/no)",
    "include_cronjobs": "Include example cronjob configuration? (yes/no)"
  }
}
